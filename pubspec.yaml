name: onedrivegallery
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cached_network_image: ^3.3.0
  flutter_riverpod: ^2.0.0
  flutter_secure_storage: ^9.2.4 # Para almacenar el token de manera segura
  flutter_appauth: ^6.0.0
  flutter_dotenv: ^5.1.0
  english_words: ^4.0.0
  provider: ^6.0.0
  url_launcher: ^6.2.5
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  aad_oauth: ^1.0.1  
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
